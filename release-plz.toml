[workspace]
release_always = false
git_release_enable = true
git_tag_enable = true
repo_url = "https://github.com/jBernavaPrah/webots-proto"
git_tag_name = "{{ package }}-v{{ version }}"
git_release_name = "{{ package }}-v{{ version }}"
pr_labels = ["release"]
publish_timeout = "10m"
